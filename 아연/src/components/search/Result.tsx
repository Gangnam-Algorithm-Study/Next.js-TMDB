import Card from '@/components/Card'
import { Movie } from '@/types/movie'

const SearchResult = ({ data }: { data: Movie[] }) => {
  console.log(data)
  return <div>dsfasd</div>
}

export default SearchResult
